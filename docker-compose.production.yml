version: "3.7"

services:
  app:
    container_name: app
    build:
      context: .
      dockerfile: docker/flask/Dockerfile
      args:
        - ENVIRONMENT=production
        - PORT=5000
    volumes:
      - .:/app
      - flask-static:/app/static
    ports:
      - 5000:5000

volumes:
    flask-static:
